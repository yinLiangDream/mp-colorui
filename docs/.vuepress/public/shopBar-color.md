```jsx
<ClShopBar
  onClickTab={(index) => {this.clickIcon(index)}}
  onClickButton={(index) => {this.clickButton(index)}}
  bgColor='gray'
  tabs={tabs}
  buttons={buttons}
/>

<ClShopBar
  onClickTab={(index) => {this.clickIcon(index)}}
  onClickButton={(index) => {this.clickButton(index)}}
  bgColor='light-blue'
  tabs={tabs}
  buttons={buttons}
/>

<ClShopBar
  onClickTab={(index) => {this.clickIcon(index)}}
  onClickButton={(index) => {this.clickButton(index)}}
  bgColor='gradualGreen'
  tabs={tabs}
  buttons={buttons}
/>
```
